/** Flows owned by the upstream template repo. Downstream forks should not modify upstream specs without coordinating. */
export const TAG_UPSTREAM = '@upstream';

/** Fork-specific flows added by downstream consumers. */
export const TAG_DOWNSTREAM = '@downstream';

/** Authentication-related flows. */
export const TAG_AUTH = '@auth';

/** Navigation/layout flows. */
export const TAG_NAV = '@nav';

/** Admin/system-administrator flows. */
export const TAG_ADMIN = '@admin';
