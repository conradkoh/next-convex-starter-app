{
  "name": "next-convex-starter-app-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "start": "node dist/bin.js",
    "prestart": "pnpm run build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.17.24",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
